{
  "name": "rs-world-ping",
  "version": "2.0.1",
  "description": "generates a list of world pings for rs classic",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node build/index.js",
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc"
  },
  "keywords": [],
  "author": "lily.slvr",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.41",
    "@types/ping": "^0.2.0",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "ping": "^0.4.1"
  }
}
